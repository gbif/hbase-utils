<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.gbif</groupId>
    <artifactId>motherpom</artifactId>
    <version>19</version>
  </parent>

  <artifactId>hbase-utils</artifactId>
  <version>0.9-SNAPSHOT</version>

  <name>GBIF Common :: HBase Utils</name>

  <properties>
    <hadoop.version>2.5.0-cdh5.2.0</hadoop.version>
    <hbase.version>0.98.6-cdh5.2.0</hbase.version>
    <hive.version>0.13.1-cdh5.2.0</hive.version>
    <oozie.version>4.0.0-cdh5.2.0</oozie.version>
    <zookeeper.version>3.4.5-cdh5.2.0</zookeeper.version>

    <junit.version>4.10</junit.version>
    <slf4j.version>1.7.7</slf4j.version>
  </properties>

  <scm>
    <connection>scm:git:git@github.com:gbif/${project.artifactId}.git</connection>
    <url>https://github.com/gbif/${project.artifactId}</url>
    <developerConnection>scm:git:git@github.com:gbif/${project.artifactId}.git</developerConnection>
    <tag>HEAD</tag>
  </scm>

  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <configuration>
          <descriptorRefs>
            <descriptorRef>jar-with-dependencies</descriptorRef>
          </descriptorRefs>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <repositories>
    <repository>
      <id>gbif-thirdparty</id>
      <url>http://repository.gbif.org/content/repositories/thirdparty</url>
    </repository>
    <repository>
      <id>gbif-all</id>
      <url>http://repository.gbif.org/content/groups/gbif</url>
    </repository>
    <repository>
      <id>cloudera</id>
      <url>https://repository.cloudera.com/content/repositories/releases/</url>
      <snapshots>
        <enabled>true</enabled>
        <updatePolicy>always</updatePolicy>
      </snapshots>
      <releases>
        <enabled>true</enabled>
      </releases>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>org.apache.hbase</groupId>
      <artifactId>hbase-common</artifactId>
      <version>${hbase.version}</version>
      <exclusions>
        <!-- ignore MR1 artifacts -->
        <exclusion>
          <groupId>commons-logging</groupId>
          <artifactId>commons-logging</artifactId>
        </exclusion>
        <exclusion>
          <groupId>org.apache.hadoop</groupId>
          <artifactId>hadoop-core</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>org.apache.hbase</groupId>
      <artifactId>hbase-client</artifactId>
      <version>${hbase.version}</version>
      <exclusions>
        <!-- ignore MR1 artifacts -->
        <exclusion>
          <groupId>org.apache.hadoop</groupId>
          <artifactId>hadoop-core</artifactId>
        </exclusion>
        <!-- remove logging which we'll ship to logback -->
        <exclusion>
          <groupId>commons-logging</groupId>
          <artifactId>commons-logging</artifactId>
        </exclusion>
        <exclusion>
          <groupId>org.slf4j</groupId>
          <artifactId>slf4j-log4j12</artifactId>
        </exclusion>
        <exclusion>
          <groupId>log4j</groupId>
          <artifactId>log4j</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>org.apache.hbase</groupId>
      <artifactId>hbase-testing-util</artifactId>
      <version>${hbase.version}</version>
      <scope>test</scope>
      <!-- The following are to exclude MR1 artifacts -->
      <exclusions>
        <!-- ignore MR1 artifacts -->
        <exclusion>
          <groupId>org.apache.hadoop</groupId>
          <artifactId>hadoop-core</artifactId>
        </exclusion>
        <!-- ignore MR1 artifacts -->
        <exclusion>
          <groupId>org.apache.hadoop</groupId>
          <artifactId>hadoop-minicluster</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-common</artifactId>
      <version>${hadoop.version}</version>
      <exclusions>
        <exclusion>
          <groupId>org.slf4j</groupId>
          <artifactId>slf4j-log4j12</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j.version}</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>log4j-over-slf4j</artifactId>
      <version>${slf4j.version}</version>
    </dependency>
  </dependencies>

  <profiles>
    <profile>
      <id>prod</id>
      <properties>
        <!-- cluster -->
        <zookeeper.quorum>c1n10.gbif.org:2181,c1n9.gbif.org:2181,c1n8.gbif.org:2181</zookeeper.quorum>
        <hadoop.jobtracker>c1n10.gbif.org:8021</hadoop.jobtracker>
        <hdfs.namenode>hdfs://c1n8.gbif.org:8020</hdfs.namenode>
        <oozie.url>http://c1n8.gbif.org:11000/oozie</oozie.url>
        <hive.metastore.uris>thrift://c1n8.gbif.org:9083</hive.metastore.uris>

        <!-- cluster version -->
        <hadoop.version>2.5.0-cdh5.2.0</hadoop.version>
        <hbase.version>0.98.6-cdh5.2.0</hbase.version>
        <hive.version>0.13.1-cdh5.2.0</hive.version>
        <oozie.version>4.0.0-cdh5.2.0</oozie.version>
        <zookeeper.version>3.4.5-cdh5.2.0</zookeeper.version>
      </properties>
    </profile>

    <profile>
      <id>dev</id>
      <properties>
        <!-- cluster -->
        <zookeeper.quorum>c1n1.gbif.org:2181,c1n2.gbif.org:2181,c1n3.gbif.org:2181</zookeeper.quorum>
        <hadoop.jobtracker>c1n3.gbif.org:8021</hadoop.jobtracker>
        <hdfs.namenode>hdfs://c1n1.gbif.org:8020</hdfs.namenode>
        <oozie.url>http://c1n1.gbif.org:11000/oozie</oozie.url>
        <hive.metastore.uris>thrift://c1n1.gbif.org:9083</hive.metastore.uris>

        <!-- cluster version -->
        <hadoop.version>2.5.0-cdh5.2.0</hadoop.version>
        <hbase.version>0.98.6-cdh5.2.0</hbase.version>
        <hive.version>0.13.1-cdh5.2.0</hive.version>
        <oozie.version>4.0.0-cdh5.2.0</oozie.version>
        <zookeeper.version>3.4.5-cdh5.2.0</zookeeper.version>
      </properties>
    </profile>
  </profiles>

</project>
